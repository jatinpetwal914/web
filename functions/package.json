{
  "name": "phadizon-functions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint . --ext .ts"
  },
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.4.0",
    "stripe": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "eslint": "^8.36.0",
    "eslint-config-google": "^0.14.0"
  }
}